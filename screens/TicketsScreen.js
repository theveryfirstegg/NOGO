import { View, Text } from 'react-native'
import styles from './TicketsScreen.styles'

const TicketsScreen = () => (
	<View style={styles.container}>
		<Text>TicketsScreenx</Text>
	</View>
)

export default TicketsScreen